package net.project104.civyshkbirds;

import android.app.Application;
/*
import net.project104.swartznetlibrary.JmdHelper;
import net.project104.swartznetlibrary.NetHandler;

public class MyApplication extends Application {
    //NsdHelper mNsdHelper = null;//Broken Android NSD
    JmdHelper mJmdHelper = null;
    NetHandler mNetHandler = null;
    NetGame mNetGame = null;

    public NetGame getNetGame(String playerName, NetGame.RoomListener roomListener){
        if(mNetGame == null){
            mNetGame = new NetGame(this, playerName, roomListener);
        }
        return  mNetGame;
    }

    public JmdHelper getNsdHelper(){
        if(mJmdHelper == null){
            mJmdHelper = new JmdHelper(this, "TODO CJhange tHIs");//todo
        }
        return mJmdHelper;
    }

    public NetHandler getNetConnection(NetHandler.ProtocolManager mgr){
        if(mNetHandler == null){
            mNetHandler = new NetHandler(mgr);
        }
        return mNetHandler;
    }

}
*/