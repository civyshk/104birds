<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/rootView"
    android:layout_height="match_parent"
    android:layout_width="match_parent"
    android:padding="0dp"
    android:background="@color/sky_0"
    tools:context="net.project104.civyshkbirds.ActivityCards">
<!--
    <ImageView android:id="@+id/ivStars"
        android:src="@drawable/stars"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:alpha="0.0"/>
-->
    <FrameLayout android:id="@+id/rootSingleFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        >

    </FrameLayout>

    <!--This Layout should have 1 child on small screens; bigger ones get other layout (large)
    - activity_cards
      - fragment_cards (allways)
      - fragment_card (big screens)

      android:layout_marginTop="@dimen/activity_vertical_margin"
        android:layout_marginBottom="@dimen/activity_vertical_margin"
        android:layout_marginLeft="@dimen/activity_horizontal_margin"
        android:layout_marginRight="@dimen/activity_horizontal_margin"

    -->

</FrameLayout>
